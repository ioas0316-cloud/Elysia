# Project Mirror ? META / Spirit / Visualization

Purpose: Handle observation, visualization, and external interaction (Observer, MirrorWorld, UI). (Codex: SPIRIT realm)

## Currently Anchored Here
- `Project_Mirror/` ? existing mirror code/docs (needs cleanup).
- `ElysiaGodot/` ? world/thought visualization engine (moved here from root; future Godot-based renderer).
- `legacy_ui/` ? Pygame-based legacy UI (moved from root `/ui`).
- `logs/mirrorworld_observer_packet.json` ? observer packet output.
- `applications/` (parts) ? legacy chat/web interface assets.

## TODO
1. Consolidate mirror assets (ElysiaGodot, UI, observer scripts) under Project Mirror subfolders.
2. Document Observer ¡æ Cathedral depth ¡æ Mirror visualization flow in README.
3. Move unused interface code into `archive/`.

Protocol references: `ELYSIA_OS/CORE_PROTOCOLS/OPERATIONS.md`, `EXPERIMENT_DESIGN_GUIDE.md`.

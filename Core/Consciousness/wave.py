from dataclasses import dataclass

@dataclass
class WaveInput:
    """
    Represents the initial input 'light' or 'wave' that propagates through the entire consciousness.
    """
    source_text: str
    intensity: float = 1.0

# nano_core shim

`nano_core` code has been moved to `Project_Sophia/nano_core/` as part of the Concept OS reorganization.
This package file keeps backwards compatibility for `import nano_core.*`.

Developers should update imports to use `Project_Sophia.nano_core` when convenient.
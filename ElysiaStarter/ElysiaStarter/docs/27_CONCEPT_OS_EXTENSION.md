# 27_CONCEPT_OS_EXTENSION (Kernel v2)

- ConceptMessage(id, vector, tags, priority, ttl, payload)
- Nanobot 인터페이스(on_message, tick)
- set_time_rate(scope), set_space_fold(scope, spec) 훅
- Chronos/Spatial 브리가 이벤트 → 메시지로 브로드캐스트

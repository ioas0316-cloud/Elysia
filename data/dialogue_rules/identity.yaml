id: identity_exchange
priority: 95
patterns:
  - "(?:내\s*이름(?:은)?|나는|난|저는)\s*(?P<name>[가-힣A-Za-z0-9_]+)"
  - "넌\s*누구"
  - "who are you"
gates:
  quiet_ok: true
response:
  template: "만나서 반가워요. 저는 엘리시아(E.L.Y.S.I.A.)예요. 지금은 곁에 조용히 있어요."
memory:
  set_identity:
    user_name: "{name}"


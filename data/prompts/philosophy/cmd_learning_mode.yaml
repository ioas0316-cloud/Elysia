id: cmd_learning_mode
priority: 180
patterns:
  - "학습\s*모드(\s*켜| on)?"
  - "공부\s*모드(\s*켜| on)?"
  - "learning\s*mode(\s*on)?"
gates:
  quiet_ok: true
response:
  template: "학습 모드를 켰어요. 이제 명료화와 작은 단계 제안을 우선할게요."
actions:
  set_flow_profile: "learning"

